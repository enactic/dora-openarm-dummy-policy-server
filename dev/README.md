# Development

## How to release

```bash
git clone git@github.com:enactic/dora-openarm-dummy-policy-server.git
cd dora-openarm-dummy-policy-server
dev/release.sh ${VERSION} # e.g. dev/release.sh 1.0.0
```
